# Which locale the unqualified res/values directory holds.
#
# Required by `generateLocaleConfig` — without it the build fails at
# :app:extractDebugSupportedLocales. The source strings Crowdin translates are English.
unqualifiedResLocale=en-US
